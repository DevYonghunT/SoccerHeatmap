name: kickoff_soccer
description: "KICKOFF - Soccer Heatmap Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # UI
  flutter_svg: ^2.0.9
  lucide_icons: ^0.257.0
  google_fonts: ^6.1.0

  # State Management
  flutter_riverpod: ^2.4.9

  # Routing
  go_router: ^13.0.0

  # Local Storage
  hive_flutter: ^1.1.0
  hive: ^2.2.3

  # Utilities
  intl: ^0.19.0
  uuid: ^4.2.2

  # Share
  share_plus: ^7.2.1
  path_provider: ^2.1.1

  # Screenshot
  screenshot: ^3.0.0

  # Ads
  google_mobile_ads: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # hive_generator와 build_runner 제거됨
  # - 수동 TypeAdapter 사용 중 (hive_adapters.dart)
  # - 코드 생성 불필요

flutter:
  uses-material-design: true
